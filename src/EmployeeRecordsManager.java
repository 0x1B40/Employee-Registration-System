import java.util.Scanner;
import java.util.GregorianCalendar;
import javax.swing.*;
import java.awt.*;
public class EmployeeRecordsManager {


    public static void main (String[] args) {



        //GUI: Description: select either to upload an employeeList from a file, so inside the file, the list is created, then the employees are added to the list and that is what
        // the program is reading, the other option is to create an employeeList,
        // after creating the list, the user has several options( Basically a button for each method in employeeList, and a button to terminate the program)
        // a new interface for each button clicked, addEmployeeEnd, addEmployeeAt(emp,index),deleteEmployeeAt(index),deleteEmployee(empID),updateRecord(emp,empID),


        MainmenuFrame mainmenuFrame = new MainmenuFrame();
        mainmenuFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        mainmenuFrame.setSize(500,500);
        mainmenuFrame.setVisible(true);












   }


// outside main, inside the class




}